# Sprint 3 — decisions

Log of deliberate divergences from the Paper source, and why.

| Date | Decision | Rationale | Affects |
|------|----------|-----------|---------|
| 2026-09-04 | Restyle to shadcn tokens rather than reproduce the Paper palette and type (DM Sans / Plus Jakarta Sans). | The brief is an uplift pass — structure ported, surface raised to the design system. | Idea 1, all screens |
| 2026-09-04 | Prototypes are static and hardcoded. No filter engine, no data fetching, no state beyond click-through. | Exploratory sprint, not a production build. Keeps 3 ideas × N screens affordable. | All ideas |
| 2026-09-04 | Idea 3 must not be a chat. The source AI tab already renders a transcript, so a conversational design would have duplicated the incumbent rather than explored past it. | The direction has to test something Idea 1 does not already do. | Idea 3 |
| 2026-09-06 | Prototype screens render full bleed at viewport height; all navigation moved into a floating Explorer (Cmd/Ctrl+Shift+E). No breadcrumb, frame, stepper or footer. | The source screens are a product, not a slide — a visible harness undercuts the review. The Explorer is `fixed`, so it never reflows the screen. | All ideas |
| 2026-09-06 | `AppChrome` fills the viewport instead of the source's fixed 900px height; the modal caps at 750px and shrinks below that. | The prototype has to hold up on whatever window the client opens it in. | Idea 1, all screens |
| 2026-09-06 | Sprint renumbered 2 → 3 across routes, folders and the registry. | The repo was one behind the client's numbering: their Sprint 2 was the filter-builder modal reviewed on 2 Sep. This work is Sprint 3. References to what the client saw in their Sprint 1 and Sprint 2 keep the original numbers. | Whole repo |
| 2026-09-06 | Idea 1 is reframed as the incumbent rather than a contender. Screens unchanged; `premise`, `rationale` and tags say it is the rejected baseline. | The alternatives need something concrete to be argued against, and the objections have to stay visible in the flow. | Idea 1 |
| 2026-09-06 | The applied-filter bar is `sticky` inside the scrolling content area. | The bar scrolled away with the table, which the real product would not do. | Idea 1 — applied, many-filters, group-by, filter-bar-dropdown |
| 2026-09-06 | Cascade popovers are flex columns with a capped height and an internally scrolling value list. | At fixed offsets they were clipped by the modal's `overflow-hidden` on windows shorter than ~800px. Unchanged at a normal window height. | Idea 1 — manual-attributes, manual-values, manual-selected, filter-bar-dropdown |
| 2026-09-06 | Explorer's back control routes explicitly to the index instead of calling `router.back()`. | A screen URL opened cold has no history, so the button did nothing visible. | Explorer |
| 2026-09-06 | Idea 3 built as a single editable sentence, not the multi-turn conversational refinement first specified. No transcript, no turns, no branching — natural language is read once and comes back as one line of English whose values, operators and joining words are all dropdowns. | Multi-turn narrowing is what the source AI tab already does, so building it would have re-argued the incumbent. The sentence tests the harder question instead: whether natural language can *be* the filter UI rather than sit beside it. The reversibility the original entry wanted survives as clause dismissal, value clearing and undo. | Idea 3 |
| 2026-09-06 | Idea 3's sixteen sample rows are filtered in memory against the sentence, and each row carries the attribute every clause tests. | Editing the sentence moved the count but not the table — remove Phase III and Phase III drugs stayed on screen. In a direction whose whole claim is that the sentence *is* the query, that reads as broken. Still no filter engine: a fixed array and a predicate. | Idea 3 — sentence |
| 2026-09-06 | Idea 4's widest record sits eleventh in the row order so the expanded detail band clears the open column menu. | The default frame had the popover on top of the detail band, so the two features the direction is arguing for obscured each other in the first thing a reviewer sees. | Idea 4 — grid |
| 2026-09-06 | Idea 2's filter panel shows two columns below a 1450px window instead of three, with the breadcrumb carrying the extra depth. | Three columns ellipsised the long labels — `Therapy Area / Indic…`, `Musculoskel…`. Two readable columns say more than three cramped ones, and the breadcrumb is already a live control. | Idea 2 — screener |
| 2026-09-06 | Idea 2's attribute column counts distinct values rather than drugs. | The drug count read 146 against nearly every attribute, which invites the question and answers nothing. Value counts vary and say how far an attribute can actually split the set. Free-text attributes carry an em dash. | Idea 2 — screener |
| 2026-09-06 | One shared `ProductChrome` replaces the four per-idea `AppChrome` components. Wordmark, area tabs, global search, notifications and account are now identical on every screen. | Each direction had invented its own header, with different entity lists and different labels. Shown in one session they read as three or four products rather than four proposals for one, which undercuts the comparison. | All ideas |
| 2026-09-06 | The canonical area list is the real product's eight — Companies, Drugs, Licensing Opportunities, Regulatory Milestones, Sales and Forecast, Drugs by Manufacturer, NPV, Advanced Company Watchlist. The invented five-item nav (Drugs / Companies / Deals / Clinical Trials / Reports) is gone. | Drugs is one of eight peers in the live platform. No second tier was added above them: inventing a product-level nav would repeat exactly the mistake being fixed. | All ideas |
| 2026-09-06 | The chrome is two rows — identity, search and account above; the eight areas below. | `Advanced Company Watchlist` is twenty-six characters. The eight areas will not sit inline beside a wordmark and a 264px search field without being abbreviated into names the product does not use. Idea 4 had already reached this layout independently. | All ideas |
| 2026-09-06 | The header search is a slot (`search`), not a fixed placeholder box. | One direction is going to argue that the platform's cross-entity natural-language search is good and cannot feed the screener. That argument has to be makeable by wiring the real field, not by drawing a second header. | All ideas |
| 2026-09-06 | Idea 1 keeps the shared chrome and passes `body="scroll"` rather than keeping a plainer header of its own. | Its plainness belongs below the chrome — the incumbent's filter bar and table — not in the shell. The shell is the same product in all four directions. Its filter chips keep the plainer look through `FilterPill variant="muted"`. | Idea 1 |
| 2026-09-06 | `StageBadge`, `FilterPill` and `OperatorWord` lifted to `src/components/prototype/`. Idea 3's interactive operator words, Idea 1's Boolean `OperatorPill` and Idea 4's `ValuePills` stay local. | The first three were drawn three or four times with small differences a client would read as inconsistency. The last three look similar but are load-bearing to one direction each; merging them would flatten what the directions are testing. | Ideas 1–4 |
| 2026-09-06 | Idea 1's results table still shows development stage as a plain cell, not a `StageBadge`. | It is a faithful port of the design being argued against; the incumbent's grid has no status badges. | Idea 1 |
| 2026-09-07 | Idea 3's sample grows from sixteen rows to seventy-eight, spread across ten therapy descriptors, twenty-one targets, all five molecule types and the whole pipeline from Discovery to Withdrawn; the value lists for Drug Descriptor, Target and ATC Classification grow with it, and the results table gains a Drug Descriptor column while losing its sort arrows. `screenDrugs` reconciles the count against the rows — no matching row means zero, and the count is floored at the number drawn. | The sixteen rows were one neighbourhood — generic anti-inflammatory ACTG2 small molecules — so any query outside it (`phase 3 oncology`, `monoclonal antibodies in preclinical`) produced a confident four-digit count above an empty table. In the direction whose whole argument is that the sentence can be trusted, a number the rows cannot corroborate is the first thing a sceptical reader finds. Every value in the sample is a canonical option, so everything visible in the table is also askable in the sentence. The sort arrows go because they did nothing, and their width is what lets eight columns fit a 1440px window without the horizontal scroll the incumbent is criticised for; the sortable grid is Idea 4's argument to make. | Idea 3 — start, sentence |
| 2026-09-07 | Idea 3's Development Stage values are listed in pipeline order — Discovery, Preclinical, Phase I–III, Pre-registration, Marketed, Withdrawn — rather than by descending count like the other attributes. `Marketed` and `Withdrawn` carry the noun in the sentence: `in the Marketed stage`. | A clause renders its values in its own option order, so `phase 1/2` resolved to the sentence "in Phase II or Phase I". Pipeline order is also how an analyst reads a stage. Separately, `in Marketed` and `in Withdrawn` read like a form field beside `in Phase II`, which undercuts the claim that the query is just English — the two values that are statuses rather than steps now carry their own noun. | Idea 3 |
| 2026-09-07 | The prototypes are no longer stock shadcn. A cerulean accent — `oklch(0.52 0.16 253)`, wired to `--primary` and `--ring` — carries primary actions, active and selected states, filter chips that are on, focus rings and the agent's accents. Black is no longer the active-state colour anywhere. `CLAUDE.md`'s "should read as stock shadcn" instruction is superseded. | **The client asked for it.** The set read as washed-out wireframes: everything neutral grey or black on white, no layering between page, chrome and panel, and nothing that could be emphasised except by weight. The reference point is GlobalData's own live platform, which runs on a corporate navy; ours is a slightly lighter, more saturated relative, so it reads as the same family without looking dated. One intense colour used deliberately, not five. | Ideas 1–4 |
| 2026-09-07 | A five-step surface ladder — `surface-sunken`, `-page`, `-chrome`, `-panel`, `-raised` — plus three rule weights, `border-hairline` / `border-border` / `border-edge`. The six ad-hoc `bg-muted/20…/70` recess values across the four directions are gone. | The designer's framing: communicate as if this were a dark-mode UI and execute it in light mode. In dark mode an AMOLED page, a deep-grey panel and a lighter card separate on their own; in light mode nothing did, because `--background`, `--card` and `--popover` were all pure white and `--muted`, `--accent` and `--secondary` were all the same grey. The four directions had each invented their own layers with opacity modifiers, and no two agreed — three different table-header greys alone. Light mode stays: the live platform is light and the client is not being surprised with a dark UI. | Ideas 1–4, harness |
| 2026-09-07 | `StageBadge` colours development stage by pipeline position rather than rendering thirteen identical grey outlines: one hue deepening from Phase I to Phase IV / Pre-registration, green for Approved and Marketed, rose for Withdrawn / Discontinued / Suspended, grey for Discovery and Preclinical. Idea 1 still shows stage as a plain cell. | Stage is an ordering an analyst reads constantly, and identical badges made them parse it one row at a time. Encoding position makes maturity scannable straight down a column with no key to learn. Phase IV and Pre-registration share a rung because five steps of one hue is more than an eleven-pixel badge can hold apart. Fills stay under 0.06 chroma so a column of them cannot compete with the solid accent a few pixels away. | Ideas 2, 3, 4 |
| 2026-09-07 | Negation gets its own tone. `FilterPill variant="excluded"`, Idea 2's `is not` word and negated Miller column, Idea 3's `excluding` operator and the pills after it all render in rose rather than in the same grey as an include. | An excluded filter that looks identical to an included one makes the whole Boolean depend on three characters of English. Idea 3 ships with a live exclusion in its worked example and Idea 2's seeded state has one; in both, the entire negation signal was the word `not`. Idea 1's Boolean `NOT` chip stays plain — it is the incumbent, and its illegibility is the argument. | Ideas 2, 3 |
| 2026-09-07 | The timing vocabulary from Idea 3's resolve is extracted into `src/components/prototype/motion.ts` and spent in Idea 2 (Miller rows light as the agent ticks them) and Idea 4 (a proposal card lights as it lands on the grid). Idea 3 now reads its own marks from the shared module. | The client singled out the resolve transition. What makes it work is the beat, not the effect — 180 / 760 / 1400 on a hard-decelerating curve, a 70ms stagger capped at eight. Four directions moving at three different speeds read as three products. `useSettle` never gates rendering, so a reduced-motion reader still sees every value; all three class fragments carry `motion-reduce:transition-none`. | Ideas 2, 3, 4 |
| 2026-09-07 | An editorial pass deleted the interface's explanatory copy: hint placeholders, `Try` eyebrows over visible examples, glosses on `is` / `is not` / `or` / `and`, Boolean tutorials in zero-result states, and the agent preambles in Idea 4 that restated the proposal card underneath them. Honest disclosures and messages reporting a real outcome were kept, shortened where they were overlong. | Every string has to earn its place, and the same "or means any, and means all" gloss appeared five times across three directions. Idea 3's composer placeholder is the one hint restored: that field has no border, no label and no icon, so the placeholder is the only thing naming the control the direction rests on. | Ideas 1–4 |
| 2026-09-07 | The accent becomes the client's brand primary, `#0034ec` — `--brand: oklch(0.465 0.27 264)`. The cerulean `oklch(0.52 0.16 253)` is gone. The six supporting tokens are re-derived rather than hue-rotated: `brand-strong` `oklch(0.405 0.235 264)`, `brand-ink` `oklch(0.43 0.18 264)`, `brand-border` `oklch(0.855 0.062 264)`, `brand-tint` `oklch(0.953 0.022 264)`, `brand-wash` `oklch(0.973 0.012 264)`, `--ring` `oklch(0.6 0.205 264)`. Charts follow to hue 264. | The client gave us the brand primary, so it is not a preference any more. It is not a drop-in swap: `#0034ec` sits **on** the sRGB gamut wall — 0.27 is the most chroma hue 264 can hold at any lightness — so there is no headroom above it and the scale has to be built downward for the solids and toward white for the tints. That ceiling also caps the pale end: `tint` and `wash` are at the maximum chroma their lightness allows (0.022 at L 0.953, 0.012 at L 0.973), so a selected chip reads as the new brand by **hue**, not by intensity, and cannot be made more saturated without going darker. `brand-ink` is a darker, calmer relative rather than the raw brand: the brand clears 4.5:1 as text on white at 7.5:1, but 0.27 chroma at 12px shimmers and makes a coloured word look like a button. Contrast: white on `brand` 7.5:1, white on `brand-strong` 9.6:1, `brand-ink` on white 8.5:1, on `brand-tint` 7.4:1, on `brand-wash` 7.8:1, on `brand-border` 5.4:1. | Ideas 1–4, harness |
| 2026-09-07 | The stage ramp is rebuilt on hue 264. Chroma per rung is unchanged (0.015 / 0.029 / 0.043 / 0.057); the lightness steps widen from 0.024 / 0.028 / 0.028 to 0.030 / 0.032 / 0.032. Green for Approved and Marketed and rose for Withdrawn / Discontinued / Suspended are untouched. `--stage-dormant-ink` darkens from L 0.6 to L 0.54. | The blue gamut is narrower at 264 than at 253 — 0.062 chroma is the ceiling at L 0.874 — so the ramp cannot buy separation with saturation and takes it from lightness instead. That is also the right trade now a solid accent chip sits nearby at 0.27: the four rungs read one notch further apart top to bottom and no louder side to side, and every fill stays under 0.06 as before. Archived was darkened while the ramp was open: at L 0.6 the label sat at 3.6:1 on its own fill, under AA for 11px text. It is still the faintest badge in the set at 4.6:1. | Ideas 2, 3, 4 |
| 2026-09-07 | The negation family moves up a step: `--negative` `oklch(0.947 0.026 22)`, `--negative-ink` `oklch(0.475 0.155 22)`, `--negative-border` `oklch(0.878 0.055 22)`. | The new accent leans violet — hue 264 against the old 253 — which closes eleven degrees of the gap to this rose, and the accent's own chips gained a stronger border and a deeper ink at the same time. Left where it was, an `is not` chip would have read as the greyer, weaker sibling of an `is` chip rather than as its opposite. The fill stays under 0.03 so a bar of exclusions still does not read as an error state. | Ideas 2, 3 |
| 2026-09-07 | Two call sites the token layer could not reach. `button.tsx`'s default variant hovers to `bg-brand-strong` instead of `bg-primary/80`. Idea 2's Miller attribute count badge changes from a solid `bg-brand` disc to `bg-brand-border` with `text-brand-ink`. | An alpha step over a light surface makes the accent *paler* on hover, which at 0.27 chroma reads as the button losing interest rather than responding; no token value can fix a `/80`, so the deeper step the ramp already carries is named directly. The count badge is the "less of it" case: solid accent put a stack of four filled discs down the left of the filter panel, which was busy at the old cerulean and at `#0034ec` competes with the checkboxes two columns over — the one control on that screen that has to be solid. Tinted, it still says the attribute is on. Everything else downstream reads the tokens, so the swap needed no other call-site changes. | Idea 2 — screener; all buttons |
| 2026-09-13 | Idea 3's colour narrows from CLAUDE.md's general accent rule — primary actions, active/selected states, chips, focus rings, agent accents — to links and buttons only. The active `Sentence`/`Filters` tab and the resolved drug count go back to `text-foreground`; the results-row hover, the resolving pulse dot, and the dashed and suggested-query hover states in the filter and composer views all lose their blue. Chips (`ValuePill`, `FilterPill`) keep `brand-tint` at rest — ticking or opening one is a button press, so they read as "on" under the narrower rule too; only their hover calms, moving from a hard border fill to `brand-wash`/`negative` with `shadow-panel`. | The client's line-by-line read of this screen was that colour should mean "you can act on this," not "the model resolved this correctly." A tab you are already on and a count you are only reading are neither, and leaving them blue spent the accent's weight on information instead of the two things on the screen that are genuinely a click — Export and the value pills. Scoped to Idea 3 rather than folded into CLAUDE.md, since ideas 1, 2 and 4 have not been checked against the same question. Open item in NEXT-SESSION.md. | Idea 3 — sentence |
| 2026-09-13 | A neutral disabled state — `--disabled` / `--disabled-foreground`, a flat grey with no hue — replaces `disabled:opacity-50` on every shared and hand-rolled button across all four ideas. `button.tsx` is hand-edited again, alongside the hover fix logged above. | Opacity over a solid `brand` fill at 0.27 chroma does not read as "unavailable" — it reads as the same button, dimmed, which looks like a rendering fault rather than a state. A flat grey token removes that ambiguity once, in the base component, instead of a per-button opacity guess repeated four times. | Ideas 1–4 — every disabled button |
| 2026-09-13 | Idea 3's results table reverses two calls from 2026-09-07: the header goes back to opaque white (`bg-surface-panel`, was the frosted `bg-surface-sunken backdrop-blur`) inside a `bg-surface-chrome` body, and a real column sort returns — click cycles ascending, descending, off, the active column showing an arrow and the rest a chevron on hover. Sort only; no other table control returns yet. | The sunken, blurred head read as a separate, slightly grubby surface sitting on the table rather than the table's own head, the opposite of the white-forward hierarchy the surface ladder is meant to buy. The sort arrows were dropped on 2026-09-07 because they did nothing; now that a real sort exists behind them, giving the arrows back is honest rather than decorative, and the column widths were rechecked to still fit eight columns at 1440px. | Idea 3 — sentence |
| 2026-09-13 | `edit as text`, with its pen icon, becomes a plain text link, `Edit` — `text-brand-ink`, underlines on hover — placed at the end of the row it edits rather than beside the toggle. | Feedback dictated on the screen read the pen-plus-label as "editor's text," which meant the combination was not landing as an edit action. An underlined link is a shape a reader already knows without a caption, which is the copy rule this repo already keeps — don't explain a control a competent user can read on sight. | Idea 3 — sentence |
| 2026-09-13 | A chip's right margin moves from a symmetric `-mx-0.5` to `-ml-0.5 mr-1`, and the comma after a clause becomes its own span (`mr-1`) rather than riding on the noun before it — `grammar.ts`'s `SentencePiece` now returns `noun` and `comma` separately, alongside the `punctuation` field `clausesToProse` already used. | Feedback dictated as "corners after filter fields" was chips crowding their trailing comma and the word that followed. Splitting the comma out gives it its own spacing without touching the noun's margin, for a sentence whose whole argument is that it reads as comfortable running prose. | Idea 3 — sentence |
| 2026-09-13 | One alignment rule for Idea 3's card: section padding (24px) plus the card's 1px border plus its inner padding (20px) puts both the input's leading text and the Export button's trailing edge at 45px from the section edge; the results table's first and last cells now pad to that same 45px instead of a flat 24px. | The card and the table beneath it were sitting on two different grids, so the eye had two left edges to reconcile on one screen. One measured number, spent in both places, closes that seam without redesigning either. | Idea 3 — sentence, results table |
| 2026-09-14 | The brand accent is toned down, same hue 264, across all four ideas — a calmer derivative of the client's `#0034ec` rather than the raw value. `--brand` `oklch(0.465 0.27 264)` → `oklch(0.49 0.205 264)` (`#2052d2`); `brand-strong` `0.405 0.235` → `0.455 0.19`; a new `brand-pressed`, `oklch(0.42 0.175 264)`; `brand-ink` `0.43 0.18` → `0.445 0.15`; `brand-border` `0.855 0.062` → `0.86 0.052`; `brand-tint` `0.953 0.022` → `0.955 0.02`; `brand-wash` `0.973 0.012` → `0.975 0.011`. Charts follow: `chart-1` is now `var(--brand)`, `chart-2`–`5` step down at 0.165/0.12/0.08/0.045 chroma. | The client asked for less saturation than the exact `#0034ec`. Contrast still clears comfortably: white on `brand` 6.60:1 (was 7.73), `brand-ink` on white 7.83:1. Not yet reviewed in a browser — see `NEXT-SESSION.md`. | Ideas 1–4, harness |
| 2026-09-14 | Primary button states are now derived steps off `brand` rather than opacity or grey: rest `brand`, hover `brand-strong` (one step — lightness −0.035, chroma −0.015), pressed `brand-pressed` (the same step again). Disabled is `brand-disabled` (`oklch(0.915 0.03 264)`) for the fill with `brand-disabled-foreground` (`oklch(0.57 0.08 264)`) carrying the label — a pale brand blue, not grey, with no shadow, no press nudge and cursor `not-allowed`. Outline, ghost and secondary variants disable more simply, keeping their resting fill and dropping only the label, to a neutral `control-disabled-foreground` (`oklch(0.63 0.01 258)`). This reverses the 2026-09-13 row below: the flat neutral `--disabled` / `--disabled-foreground` pair is gone. | Client feedback on the grey disabled state was blunt — too greyed out, and the white label was literally invisible. A pale step of the brand itself reads as the same button switched off rather than a different, broken one. Contrast: white on hover 7.64:1, on pressed 8.84:1; disabled label on disabled fill 3.49:1; disabled fill against white 1.29:1 and against the page grey 1.17:1, so the label is carrying the "off" read there, not the fill. | Ideas 1–4 — every button |
| 2026-09-14 | The invisible white label above was not a token problem. `button.tsx`'s class string had a missing space — `disabled:text-disabled-foregroundaria-invalid:...` — so the label class never compiled, and the button fell back to its default text colour sitting on a light disabled fill. | Worth its own line: a plausible design fix (soften the grey) was already on the table before the actual cause — a string concatenation bug, not a colour choice — turned up. Fixed alongside the button.tsx edit above. | button.tsx, all four ideas |
| 2026-09-14 | Idea 3 moves onto the repo-wide mid-fidelity wireframe colour rule (see `CLAUDE.md`). The `Sentence`/`Filters` active tab is now `bg-selected` (a black fill that day; `--selected` now aliases `brand`, so the token is solid brand), with a grey hover on the inactive tab; the `Add …` and `Did you mean` suggestion hovers go grey; the `GlobalSearch` row hover goes grey with a muted arrow; a ticked `ValueMenu` checkbox is `bg-selected`. Value pills stay blue chips at rest but no longer change fill on hover or open — they only lift, with `shadow-panel`. `Resolving`'s stage-1 highlight goes grey; stage-2 still lands blue chips. Brand is left in Idea 3 only on-chip and on the `Edit` link. | Supersedes part of the 2026-09-13 row above, which scoped colour to links and buttons only and had the active tab as black text on white with a `brand-wash` chip hover — both the tab treatment and the chip hover read differently now the rule applies repo-wide rather than as an Idea 3 special case. | Idea 3 — sentence |
| 2026-09-14 | Black selected fills are removed everywhere. `--selected` / `--selected-foreground` / `--selected-hover` now alias `brand` / `--primary-foreground` / `brand-strong` instead of a black fill. Checked controls — checkbox, radio, switch, progress — read solid brand across all four ideas with no call-site changes; selected tabs, toggles and options take the washed brand (`bg-brand-tint`, `border-brand-border`, `text-foreground`). | The client reversed the black-fill call from earlier the same day: the calmer brand and the washed secondary blue both read well, but black sitting next to blue read busy rather than deliberate. Supersedes the black-fill clause of the wireframe rule logged in the row above. | Ideas 1–4, harness |
| 2026-09-14 | Idea 3's `Sentence`/`Filters` toggle goes back to a white fill, black text and `shadow-panel` on the active segment, reversing this morning's `bg-selected` black fill. The right rail (count, Export, Undo, Clear all) goes back to white too, separated from the card by a darker `border-edge` divider, with a grey hover on Undo and Clear all. The `Edit` link now reads `text-brand` with a `brand-strong` underline on hover, rather than `brand-ink`. Value pills gain a hover-reveal close `×`: in Filters view it sits at zero opacity inside the chip until hover or focus, so the chip's width never shifts; in Sentence view the pill is already a button, so the `×` is a sibling laid over the chevron, one fading out as the other fades in, with no reflow. Both are their own tab stop, labelled "Remove {value}", both use `liftClass` from `motion.ts`, and both call the existing `onToggleValue` — removing a clause's last value still drops the clause. The `ValueMenu` ticked checkbox is solid brand. | The page grey planned for the rail was identical to the ground around the card, so the only in-between surface was barely distinct from white — a border does the separating job a fill can't when there's no grey to spare. The toggle's black fill next to the blue chips underneath it was the same busy pairing the selected-fill reversal above addresses. The hover-reveal close keeps a value pill's resting state uncluttered while still giving each value its own remove target, rather than only removing a whole clause. Known compromises: the chevron is hidden while the `×` shows, a clause's own "remove condition" `×` can be visible at the same time, and on touch the `×` only appears on focus. | Idea 3 — sentence |
| 2026-09-14 | The repo-wide colour law settles, for Idea 4's refinement pass and everything after it: solid brand only on primary buttons, checked controls (checkbox, radio, switch, progress, via `--selected`), filter chips that are on, and links. Every other selected state — tabs, toggles, segmented options, a selected menu or popover option, the Explorer's current screen, the assistant's own toggle — takes the washed brand instead, `bg-brand-tint` with a `brand-border` edge and `text-foreground`; the `ProductChrome` underline tab keeps foreground text and puts brand only in the underline. Hover is grey (`bg-accent`/`bg-muted`), focus is the neutral `--ring`, and brand text or icons revert to foreground on hover. | Black selected fills were tried for these same secondary states earlier the same day and reversed once reviewed against the blue: in the design lead's words, it "looks very busy alongside the blue." Two intensities of one hue read as one deliberate system where black-plus-blue read as two competing systems. | Ideas 1–4, harness |
| 2026-09-14 | Idea 4's grid header collapses to two rows: a title row with Aggregate, Group, Sort, Columns and Export on the right, and a filter row beneath it carrying the applied pills. The record count moves out of the toolbar entirely, into a count-only footer rail (`N drugs match · of 46 in sample`). Aggregates become an opt-in sticky summary row (`defaultAggregates = []`) rather than four always-on footer figures, each value sitting under its own column when turned on. Row expansion — the chevron and its detail band — is gone; a multi-valued cell's `+N` now opens a popover listing every value. Filter and pin glyphs leave the header cell: a filtered column shows a count badge instead, sort is a single directional arrow, and a pinned column is held at its minimum width so the sticky checkbox and identity columns line up. | Consolidates the toolbar, the applied-filter bar and the status strip that `NEXT-SESSION.md` §2 flagged as three competing surfaces into one two-row header plus a single trailing count, and turns the row-expansion mechanic — which grew a row's height unpredictably — into a fixed-height row with detail on demand. | Idea 4 — grid |
| 2026-09-14 | Idea 4's type scale drops to two sizes: 16px for cell data and any typed input over the grid, 14px for labels, tags, header text and menu rows, nothing smaller. Header labels never wrap — each column's minimum width is sized to fit its label, sort arrow and filter badge on one line — so the grid scrolls horizontally past that width, with the row checkbox and Drug Name pinned to the left. This reverses the 2026-09-06 "no horizontal scroll" framing recorded below. | Legibility beats fitting: an 11px header that wraps or a cell that truncates to stay inside one viewport is harder to read than a wider table a reviewer scrolls, and the live product's own "falls off the side of the screen" complaint was about an unreadable table, not a scrolling one. | Idea 4 — grid |
| 2026-09-14 | The status bar (`StatusBar.tsx`) is deleted along with its History popover; the panel reopens only from the header's "Assistant" toggle. Everything it showed — what ran, what changed, the undo — now lives inside the agent thread as part of each turn: a thinking row, step rows in three tenses (add/adding/added), and a receipt ("Applied · 15 → 4 drugs · 1.2s") with its own Undo. A `proposed` turn whose `stateVersion` has fallen behind the grid's own count goes stale — Accept disables, "Run again" replaces it, and the turn says the grid changed since it was proposed. | Answers both bugs `NEXT-SESSION.md` §2 flagged: the transcript and the receipt could disagree because they were two records of the same event: now there is one. Deriving staleness from a version counter, rather than storing a stale flag, means a turn can never say "current" against a grid that has since moved. | Idea 4 — grid |
| 2026-09-14 | The assistant panel holds at `w-[380px]` with an `h-11` header and a 2px progress bar beneath it, live only while a turn is thinking or applying. `StageBadge`'s pipeline ramp is confirmed to sit outside the selected-state colour law above — it is data encoding, not a selection, so its tinted rungs are unaffected by what colour a checked control or a selected tab is using. | Settles the open question logged in `NEXT-SESSION.md` §8: the ramp was checked side by side with the finalised colour rule and with the calmer brand value, and neither changes the case for keeping it. | Idea 4 — grid |
| 2026-09-14 | Idea 2 inverts its split: the filter panel goes from 40% of the window to `w-[80%] min-w-[900px]`, the results pane to `flex-1 min-w-[260px]`. The `flow.ts` rationale line asserting “roughly 40 / 60” is rewritten with it. | The Miller columns are the argument of this direction — a taxonomy read across rather than down — and at 40% two of them ellipsised while the table beside them spent six columns on fields nobody was cutting on. At four fifths the whole worked example (Drugs → Therapy Area → Dermatology → Indication) is on screen at once, and the results become what they actually are here: a live count you glance at while you cut, with the full record on demand. | Idea 2 — screener |
| 2026-09-14 | The applied-filter rail, the headline count and `Clear all` move out of the results header into a new `FilterFoot` at the foot of the filter panel. The `Filters` heading and its `{n} attributes / {n} values` subtitle are deleted outright; the results pane keeps only `Showing 1–N` and its toolbar. | The count and the sentence are what the columns produced, so they belong under the columns rather than over the table — ticking a value now moves the number a few pixels from the box that was ticked, and the sentence underneath says the whole query in one line. The deleted heading labelled a panel that is plainly a filter panel, and the subtitle counted things the pills already name. | Idea 2 — screener |
| 2026-09-14 | The docked `AgentPanel` is deleted. The agent is summoned on ⌘K as a spotlight over the screen (`AgentSpotlight`, built on the previously unused `CommandDialog`), with an `Ask` button in the panel foot as its only resident trace; the run itself appears there as `AgentRun` — the step list with per-step undo — and only while a run exists. The prototype disclosure the panel carried moves into the screen `note` in `flow.ts`. | The panel read as a box rather than as a way in, and it competed with the taxonomy lists for height on a 900px laptop — the case `NEXT-SESSION.md` §1 makes. Nothing of the mechanic changed: the same authored plan drives the same columns, with the same per-step undo and the same yielding when you take over. Only the door moved. The disclosure is relocated rather than dropped, being an honest statement about what is wired. | Idea 2 — screener |
| 2026-09-14 | The results table drops from six columns to three — name, stage, company — with the other three, and every field that never had a column, in a record drawer opened from `Open` on a hovered drug name. It is modal, on shadcn `Sheet`: scrim, click-outside and Escape all close it. `Columns` in the toolbar becomes real, with a floor of two checked. This supersedes the direction's original claim that nothing is dimmed and nothing overlays anything. | At a fifth of the window six columns are six ellipses, and the client asked for two or three up front with the rest behind a panel. The record is moved, not cut. Modal on the client's instruction; hand-rolling an `aside` to avoid the scrim would have meant hand-rolling focus return and dismissal with it, against CLAUDE.md's compose-the-primitive rule. The objection this direction answers was being *covered* by a filter modal, not a record read on request — the filter columns are still never replaced. | Idea 2 — screener |
| 2026-09-14 | Idea 2's Miller attribute count badge is removed rather than restyled: the `badges` field is gone from `ColumnModel` and from both call sites. The lead lane now holds exactly one thing and is never empty — a tick box where the column ticks values into the query, a radio mark where it only navigates. An attribute holding a selection is marked by weight (`font-medium`), and how many values it holds is said once, in words, on the pills in the panel foot. Supersedes the 2026-09-07 row above, which moved the badge from a solid `bg-brand` disc to `bg-brand-border` with `text-brand-ink` and argued for keeping it. | That row treated the badge as a colour problem and tinted it down. The badge was the deeper problem: it put a number on both sides of a single label, and the count lane on the right is the one place a number belongs. Weight carries the same signal — this attribute holds values in the query — without a second number on the row or a second spend of the accent. | Idea 2 — screener |
| 2026-09-14 | Idea 2's four-column threshold moves from 1440px to 1680px (`FOUR_COLUMN_MIN_WIDTH`), its fallback from two columns to three, and the column floor from `min-w-[176px]` to `min-w-[260px]`. At the sprint's 1440px review viewport the panel therefore shows three columns, with nothing truncated. Revises the 2026-09-06 row above, which set a two-column fallback below 1450px. | Re-derived from measured Geist advance widths rather than estimated. At 16px a label wants half again the room it needed at 12px — `Musculoskeletal Disorders` measures 198px against 148px — so four columns clear every filter area, attribute, therapy area and country label only from 1680px of window. The type does not yield to the column count; the column count yields to the type, and three columns is this direction's own premise: cap the depth on screen and let the breadcrumb carry the rest. 1920px would also clear the last two long indication labels and was rejected — it takes the fourth column off every laptop in the studio to save two ellipses in the deepest column. | Idea 2 — screener |
| 2026-09-14 | Idea 2's four per-column search fields are replaced by one panel-wide field that narrows every visible column at once. The panel's order is breadcrumb, then search, then the column captions, with search and captions sharing a single `bg-surface-chrome` plate and no rule between them. The client's wording was that the search sits above everything and the headers sit under it; the breadcrumb is read as the panel's heading and kept above, since the same review asked it to carry higher hierarchy than everything around it. | The per-column fields were a mock — a `div`, an icon and a span of placeholder text, with no input, no state and no handler — so four of them promised a search the panel did not have. One field that works beats four that do not, and a column whose rows a search has emptied now says `No matches` rather than going silently blank. Putting the search above the breadcrumb instead would have buried the one line that says where in the taxonomy you are. | Idea 2 — screener |
| 2026-09-14 | The rules between Idea 2's Miller columns go from `divide-hairline` to `divide-edge`, the heaviest of the three weights. | Hairline is the weight for a line inside one surface; the gap between two columns is a boundary between two planes, which is what `edge` is for. At four columns the faint rule left the panel reading as one wide list rather than as a path taken across four. | Idea 2 — screener |
| 2026-09-14 | Idea 2's type is rebuilt on a 16px baseline: rows, table cells, pills and drawer fields at 16px, counts and status lines at 14px, uppercase captions at 12px and nothing below it, the headline count at 30px, and the trailing breadcrumb crumb at 19px semibold against 15px ancestors. Row height follows to `h-9`, tick boxes to `size-4`, count lanes and the drawer's label lane widen, and the record drawer itself goes to 440px. The shared `ProductChrome` above the screen is deliberately left at its 11–15px scale. | The screen was built at 12px and read as dense rather than quiet; the client asked for 16px as the floor with the breadcrumb and the counts above it. The chrome is shared by four directions and `prototype/README.md` forbids forking it for one screen, so the mismatch between a 16px page and a 12.5px chrome is accepted rather than fixed here. `StageBadge` stays at 11px for the same reason and is logged as open. | Idea 2 — screener |
| 2026-09-14 | Idea 4's grid takes Idea 3's table type rather than the 16/14 scale recorded above: 13px cells, 10px uppercase headers at `tracking-[0.08em]`, 12px tags, pills and count rail, and `StageBadge` back to its shared size with the `h-6 px-2 text-sm` override dropped. Lane minimums re-measured, so the default nine columns sum to 1,749px instead of 1,928px. Reverses the 2026-09-14 row above that set the two-size grid scale; the horizontal scroll and the pinned identity lanes it argued for are unchanged. | A client-requested reversal. The point of the round is consistency between the directions, not density: three tables at three sizes read as three prototypes. Measured rather than scaled — uppercase at 10px with `0.08em` tracking is barely narrower than 14px sentence case (`ROUTE OF ADMINISTRATION` saves 4px of 159), so the width came off the values, not the labels. Indication keeps `grow: 2`: at 12px a two-tag cell still wants 271px against 185px of room at its minimum, more than any other lane. The agent panel is deliberately untouched — the correction was about tables and chips. | Idea 4 — grid |
