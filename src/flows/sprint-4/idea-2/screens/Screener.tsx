import { Screener as ScreenerView } from "@/flows/sprint-4/idea-2/components/Screener"

/**
 * Every frame of Sprint 4 Idea 2, as one screen. The slug in the URL says which
 * state it opens on; from there it is one live flow.
 */
export function Screener() {
  return <ScreenerView />
}
