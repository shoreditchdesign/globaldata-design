import { PrototypeShell } from "@/flows/sprint-4/idea-1b/components/PrototypeShell"

/** The first addressable state of the Sprint 4 Idea 1 prototype. */
export function Start() {
  return <PrototypeShell />
}
